import 'package:flutter/material.dart';

class PageEngIndo extends StatefulWidget {
  @override
  _PageEngIndoState createState() => _PageEngIndoState();
}

class _PageEngIndoState extends State<PageEngIndo> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Terjemahan Inggris ke Indonesia'),
        backgroundColor: Colors.cyan,
      ),
    );
  }
}
